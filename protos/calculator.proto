syntax = "proto3";

package Calucaltor;

message CalculateOperationMessage{
    float first_number = 1;
    string oper = 2;
    float second_number = 3;
}       

message CalculatedMessage{
    float result=1;
}
service Calculator{
    rpc calc(CalculateOperationMessage) returns (CalculatedMessage) {}

}